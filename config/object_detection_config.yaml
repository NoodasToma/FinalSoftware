img_size: 416
# 0.5 (was 0.7): a duckie is a pedestrian, so favour recall — better to brake on
# a slightly uncertain box than to drive through one. filter_by_scores applies the
# same floor after NMS. Raise toward 0.6-0.7 if the bot brakes for phantom ducks.
conf_threshold: 0.5
nms_threshold: 0.45